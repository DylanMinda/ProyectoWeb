@model Spotify.Modelos.DetallesPlaylist

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>DetallesPlaylist</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="PlaylistId" class="control-label"></label>
                <select asp-for="PlaylistId" class="form-control" asp-items="ViewBag.PlaylistId"></select>
                <span asp-validation-for="PlaylistId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CancionId" class="control-label"></label>
                <select asp-for="CancionId" class="form-control" asp-items="ViewBag.CancionId"></select>
                <span asp-validation-for="CancionId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
