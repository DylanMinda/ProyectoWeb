@model IEnumerable<Spotify.Modelos.Cancion>

@if (ViewBag.Message != null)
{
    <div class="alert alert-warning">
        @Html.Raw(ViewBag.Message)  <!-- Muestra el mensaje si se ha alcanzado el límite -->
    </div>
}

<table class="table table-striped align-middle">
    <thead>
        <tr>
            <th>Código</th>
            <th>Título</th>
            <th>Duración</th>
            <th>Género</th>
            <th>Reproducción</th>
            <th>Reproducciones</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Titulo</td>
                <td>@item.Duracion.ToString(@"hh\:mm\:ss")</td>
                <td>@item.Genero</td>
                <td>
                    @* Reproductor de audio *@
                    <audio controls class="audio-player" data-id="@item.Id" style="max-width:200px;">
                        <source src="@item.ArchivoUrl" type="audio/mp3" />
                        Tu navegador no soporta el elemento
                        <code>audio</code>.
                    </audio>
                </td>
                <td>@item.TotalReproducciones</td>
                <td>
                    <a asp-action="Detalles" asp-route-id="@item.Id" class="btn btn-sm btn-info">Detalles</a>
                    <a asp-action="Eliminar" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>

@* @section Scripts {
    <script>
        document.querySelectorAll(".audio-player").forEach(audio => {
            audio.addEventListener('play', function() {
                const tiempoLimite = 80; // Pausa cada 30 segundos
                const cancionId = this.getAttribute('data-id');
                let contador = 0;

                const intervalo = setInterval(() => {
                    contador++;

                    if (contador >= tiempoLimite) {
                        this.pause();
                        alert('¡Anuncio! Cambia de plan o espera.');

                        // Llamar al backend para incrementar la reproducción
                        fetch(`/api/Canciones/${cancionId}/incrementar-reproduccion`, { method: 'POST' })
                            .then(response => {
                                if (!response.ok) {
                                    alert('Límite de reproducciones alcanzado.');
                                }
                            });

                        clearInterval(intervalo);  // Detener el intervalo
                    }
                }, 1000);  // Verificar cada segundo
            });
        });
    </script>
} *@
